📌 Updating All Enums as per Your Schema

You’re right! There are multiple Enums in your schema, and I will update all Enums to ensure they match your Swagger definition correctly.

Since your database stores Enums as Strings, I will:
✅ Place all Enums inside model/enums/
✅ Use @Enumerated(EnumType.STRING) in the Model
✅ Ensure the same Enum is used in DTOs (no duplication)
✅ Use @JsonValue and @JsonCreator for correct API serialization

⸻

📂 Updated Folder Structure

src/main/java/com/example/project/
│── controller/
│── service/
│── repository/
│── dto/
│      ├── PolicyHolderDTO.java
│── model/
│      ├── PolicyHolder.java
│      ├── enums/  ✅ **(ALL ENUMS placed here)**
│      │      ├── CustomerType.java
│      │      ├── PartyType.java
│      │      ├── CharacterSetType.java
│      │      ├── Gender.java
│      │      ├── AddressType.java
│      │      ├── PhoneType.java
│      │      ├── ContactType.java
│      │      ├── DigitalType.java
│      │      ├── ReceiptOfNotices.java
│── mapper/
│── config/
│── exception/
│── util/



⸻

🔹 Updated Enums for Your Project

Now, here are all the enums updated to follow the best practices:

1️⃣ CustomerType Enum

package com.example.project.model.enums;

import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonValue;

public enum CustomerType {
    INDIVIDUAL("individual"),
    CORPORATION("corporation");

    private final String value;

    CustomerType(String value) {
        this.value = value;
    }

    @JsonValue
    public String getValue() {
        return value;
    }

    @JsonCreator
    public static CustomerType fromValue(String value) {
        for (CustomerType type : values()) {
            if (type.value.equalsIgnoreCase(value)) {
                return type;
            }
        }
        throw new IllegalArgumentException("Invalid CustomerType: " + value);
    }
}



⸻

2️⃣ PartyType Enum

package com.example.project.model.enums;

import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonValue;

public enum PartyType {
    INDIVIDUAL("individual"),
    CORPORATION("corporation");

    private final String value;

    PartyType(String value) {
        this.value = value;
    }

    @JsonValue
    public String getValue() {
        return value;
    }

    @JsonCreator
    public static PartyType fromValue(String value) {
        for (PartyType type : values()) {
            if (type.value.equalsIgnoreCase(value)) {
                return type;
            }
        }
        throw new IllegalArgumentException("Invalid PartyType: " + value);
    }
}



⸻

3️⃣ CharacterSetType Enum

package com.example.project.model.enums;

import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonValue;

public enum CharacterSetType {
    KANJI("kanji"),
    KANA("kana"),
    ALFABET("alfabet");

    private final String value;

    CharacterSetType(String value) {
        this.value = value;
    }

    @JsonValue
    public String getValue() {
        return value;
    }

    @JsonCreator
    public static CharacterSetType fromValue(String value) {
        for (CharacterSetType type : values()) {
            if (type.value.equalsIgnoreCase(value)) {
                return type;
            }
        }
        throw new IllegalArgumentException("Invalid CharacterSetType: " + value);
    }
}



⸻

4️⃣ Gender Enum

package com.example.project.model.enums;

import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonValue;

public enum Gender {
    MALE("male"),
    FEMALE("female"),
    CORPORATION("corporation");

    private final String value;

    Gender(String value) {
        this.value = value;
    }

    @JsonValue
    public String getValue() {
        return value;
    }

    @JsonCreator
    public static Gender fromValue(String value) {
        for (Gender type : values()) {
            if (type.value.equalsIgnoreCase(value)) {
                return type;
            }
        }
        throw new IllegalArgumentException("Invalid Gender: " + value);
    }
}



⸻

5️⃣ AddressType Enum

package com.example.project.model.enums;

import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonValue;

public enum AddressType {
    HOME("home"),
    OFFICE("office"),
    OTHERS("others");

    private final String value;

    AddressType(String value) {
        this.value = value;
    }

    @JsonValue
    public String getValue() {
        return value;
    }

    @JsonCreator
    public static AddressType fromValue(String value) {
        for (AddressType type : values()) {
            if (type.value.equalsIgnoreCase(value)) {
                return type;
            }
        }
        throw new IllegalArgumentException("Invalid AddressType: " + value);
    }
}



⸻

6️⃣ PhoneType Enum

package com.example.project.model.enums;

import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonValue;

public enum PhoneType {
    MOBILE("mobile"),
    LANDLINE("landline"),
    FAX("fax");

    private final String value;

    PhoneType(String value) {
        this.value = value;
    }

    @JsonValue
    public String getValue() {
        return value;
    }

    @JsonCreator
    public static PhoneType fromValue(String value) {
        for (PhoneType type : values()) {
            if (type.value.equalsIgnoreCase(value)) {
                return type;
            }
        }
        throw new IllegalArgumentException("Invalid PhoneType: " + value);
    }
}



⸻

7️⃣ ContactType Enum

package com.example.project.model.enums;

import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonValue;

public enum ContactType {
    HOME("home"),
    OFFICE("office"),
    BUSINESS_HOURS("business_hours"),
    OTHERS("others");

    private final String value;

    ContactType(String value) {
        this.value = value;
    }

    @JsonValue
    public String getValue() {
        return value;
    }

    @JsonCreator
    public static ContactType fromValue(String value) {
        for (ContactType type : values()) {
            if (type.value.equalsIgnoreCase(value)) {
                return type;
            }
        }
        throw new IllegalArgumentException("Invalid ContactType: " + value);
    }
}



⸻

8️⃣ DigitalType Enum

package com.example.project.model.enums;

import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonValue;

public enum DigitalType {
    PORTAL_SITE("portal_site"),
    LINE("line"),
    OTHERS("others");

    private final String value;

    DigitalType(String value) {
        this.value = value;
    }

    @JsonValue
    public String getValue() {
        return value;
    }

    @JsonCreator
    public static DigitalType fromValue(String value) {
        for (DigitalType type : values()) {
            if (type.value.equalsIgnoreCase(value)) {
                return type;
            }
        }
        throw new IllegalArgumentException("Invalid DigitalType: " + value);
    }
}



⸻

9️⃣ ReceiptOfNotices Enum

package com.example.project.model.enums;

import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonValue;

public enum ReceiptOfNotices {
    WEB("web"),
    WEB_AND_MAIL("web_and_mail"),
    MAILING("mailing");

    private final String value;

    ReceiptOfNotices(String value) {
        this.value = value;
    }

    @JsonValue
    public String getValue() {
        return value;
    }

    @JsonCreator
    public static ReceiptOfNotices fromValue(String value) {
        for (ReceiptOfNotices type : values()) {
            if (type.value.equalsIgnoreCase(value)) {
                return type;
            }
        }
        throw new IllegalArgumentException("Invalid ReceiptOfNotices: " + value);
    }
}



⸻

✅ All Enums Have Been Updated!

This ensures all Enums are properly stored in the DB and used in API responses. 🚀
Would you like me to update Models, DTOs, or Mappers next?