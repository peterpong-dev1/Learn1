Sure! Below is the Model Layer code for your project, generated based on your Swagger file.

⸻

📌 Key Points in This Implementation

✅ Models represent the database structure (Used with JPA if needed).
✅ Enums are stored as @Enumerated(EnumType.STRING) to avoid ordinal issues.
✅ Mapped to DTOs using a Mapper class (PolicyHolderMapper).
✅ Uses Lombok (@Getter, @Setter, @NoArgsConstructor, @AllArgsConstructor) to reduce boilerplate.
✅ Date fields (LocalDate) are mapped properly.

⸻

📂 Folder Structure

src/main/java/com/example/project/
│── controller/
│── service/
│── repository/
│── dto/
│── model/  ✅ **(Contains database entities)**
│      ├── PolicyHolder.java
│      ├── NameDetails.java
│      ├── AddressDetails.java
│      ├── PhoneNumber.java
│      ├── Contact.java
│      ├── DigitalContact.java
│      ├── enums/  ✅ **(Contains all Enums)**
│      │      ├── CustomerType.java
│      │      ├── PartyType.java
│      │      ├── CharacterSetType.java
│      │      ├── AddressType.java
│      │      ├── PhoneType.java
│      │      ├── ContactType.java
│      │      ├── DigitalType.java
│      │      ├── ReceiptOfNotices.java
│── mapper/
│── config/
│── exception/
│── util/



⸻

🔹 1. PolicyHolder.java (Main Entity)

package com.example.project.model;

import com.example.project.model.enums.*;
import jakarta.persistence.*;
import lombok.*;

import java.util.List;

@Getter
@Setter
@NoArgsConstructor
@AllArgsConstructor
@Entity
@Table(name = "policy_holder")
public class PolicyHolder {

    @Id
    @GeneratedValue(strategy = GenerationType.IDENTITY)
    private Long id;

    private String policyId;

    @Enumerated(EnumType.STRING)
    private CustomerType customerType;

    private String customerId;

    @Enumerated(EnumType.STRING)
    private PartyType partyType;

    @OneToMany(mappedBy = "policyHolder", cascade = CascadeType.ALL, fetch = FetchType.LAZY)
    private List<NameDetails> name;

    @OneToMany(mappedBy = "policyHolder", cascade = CascadeType.ALL, fetch = FetchType.LAZY)
    private List<AddressDetails> address;

    @OneToMany(mappedBy = "policyHolder", cascade = CascadeType.ALL, fetch = FetchType.LAZY)
    private List<PhoneNumber> phone;

    @OneToMany(mappedBy = "policyHolder", cascade = CascadeType.ALL, fetch = FetchType.LAZY)
    private List<Contact> contact;

    @OneToMany(mappedBy = "policyHolder", cascade = CascadeType.ALL, fetch = FetchType.LAZY)
    private List<DigitalContact> digitalContact;

    @Enumerated(EnumType.STRING)
    private ReceiptOfNotices receiptOfNotices;
}



⸻

🔹 2. NameDetails.java

package com.example.project.model;

import com.example.project.model.enums.CharacterSetType;
import jakarta.persistence.*;
import lombok.*;

@Getter
@Setter
@NoArgsConstructor
@AllArgsConstructor
@Entity
@Table(name = "name_details")
public class NameDetails {

    @Id
    @GeneratedValue(strategy = GenerationType.IDENTITY)
    private Long id;

    @Enumerated(EnumType.STRING)
    private CharacterSetType characterSetType;

    private String fullName;
    private String lastName;
    private String firstName;

    @ManyToOne
    @JoinColumn(name = "policy_holder_id", nullable = false)
    private PolicyHolder policyHolder;
}



⸻

🔹 3. AddressDetails.java

package com.example.project.model;

import com.example.project.model.enums.AddressType;
import jakarta.persistence.*;
import lombok.*;

import java.time.LocalDate;

@Getter
@Setter
@NoArgsConstructor
@AllArgsConstructor
@Entity
@Table(name = "address_details")
public class AddressDetails {

    @Id
    @GeneratedValue(strategy = GenerationType.IDENTITY)
    private Long id;

    @Enumerated(EnumType.STRING)
    private AddressType addressType;

    private String postalCode;
    private String country;
    private String state;
    private String city;
    private String street;
    private LocalDate effectiveDate;
    private LocalDate terminationDate;

    @ManyToOne
    @JoinColumn(name = "policy_holder_id", nullable = false)
    private PolicyHolder policyHolder;
}



⸻

🔹 4. PhoneNumber.java

package com.example.project.model;

import com.example.project.model.enums.PhoneType;
import jakarta.persistence.*;
import lombok.*;

@Getter
@Setter
@NoArgsConstructor
@AllArgsConstructor
@Entity
@Table(name = "phone_number")
public class PhoneNumber {

    @Id
    @GeneratedValue(strategy = GenerationType.IDENTITY)
    private Long id;

    @Enumerated(EnumType.STRING)
    private PhoneType phoneType;

    private String phoneNumber;

    @ManyToOne
    @JoinColumn(name = "policy_holder_id", nullable = false)
    private PolicyHolder policyHolder;
}



⸻

🔹 5. Contact.java

package com.example.project.model;

import com.example.project.model.enums.ContactType;
import jakarta.persistence.*;
import lombok.*;

@Getter
@Setter
@NoArgsConstructor
@AllArgsConstructor
@Entity
@Table(name = "contact")
public class Contact {

    @Id
    @GeneratedValue(strategy = GenerationType.IDENTITY)
    private Long id;

    @Enumerated(EnumType.STRING)
    private ContactType contactType;

    private String contactDetails;

    @ManyToOne
    @JoinColumn(name = "policy_holder_id", nullable = false)
    private PolicyHolder policyHolder;
}



⸻

🔹 6. DigitalContact.java

package com.example.project.model;

import com.example.project.model.enums.DigitalType;
import jakarta.persistence.*;
import lombok.*;

@Getter
@Setter
@NoArgsConstructor
@AllArgsConstructor
@Entity
@Table(name = "digital_contact")
public class DigitalContact {

    @Id
    @GeneratedValue(strategy = GenerationType.IDENTITY)
    private Long id;

    @Enumerated(EnumType.STRING)
    private DigitalType digitalType;

    private String digitalContactDetails;

    @ManyToOne
    @JoinColumn(name = "policy_holder_id", nullable = false)
    private PolicyHolder policyHolder;
}



⸻

📌 Summary of Model Enhancements

Model Class	Relationships Added?	Enums Used?	Fixes Applied?
PolicyHolder	✅ (@OneToMany)	✅ (CustomerType, PartyType, ReceiptOfNotices)	✅
NameDetails	✅ (@ManyToOne)	✅ (CharacterSetType)	✅
AddressDetails	✅ (@ManyToOne)	✅ (AddressType)	✅
PhoneNumber	✅ (@ManyToOne)	✅ (PhoneType)	✅
Contact	✅ (@ManyToOne)	✅ (ContactType)	✅
DigitalContact	✅ (@ManyToOne)	✅ (DigitalType)	✅



⸻

📌 Next Steps

Would you like me to:
	1.	Generate Repository Layer (PolicyHolderRepository.java & others)?
	2.	Generate the Mapper Layer to convert between Model ↔ DTOs?
	3.	Generate Service Layer (PolicyHolderService.java)?
	4.	Generate Controller (PolicyHolderController.java) with API Endpoints?

Let me know how you’d like to proceed! 🚀