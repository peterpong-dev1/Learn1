public class NeoChangeFeedFunctions {

  private static final String CONFIG_CONTAINER_NAME = System.getenv("BlobConfigContainer");  // e.g. "config"
  private static final String DATA_CONTAINER_NAME   = System.getenv("BlobDataContainer");    // e.g. "tracked-data"

  // Shared across all trigger methods in this class (and safe)
  private static final BlobContainerClient CONFIG_CONTAINER =
      BlobClientProvider.container(CONFIG_CONTAINER_NAME);
  private static final BlobContainerClient DATA_CONTAINER =
      BlobClientProvider.container(DATA_CONTAINER_NAME);

  @FunctionName("TriggerForContainerA")
  public void onA(
      @CosmosDBTrigger(/* ... */) List<ObjectNode> items,
      ExecutionContext ctx) {

    for (ObjectNode doc : items) {
      String docId = doc.get("id").asText();
      // read tracked fields config from blob
      String cfgBlobName = System.getenv("TrackedFields_ContainerA"); // e.g. "containerA-tracked.json"
      BlobClient cfgBlob = CONFIG_CONTAINER.getBlobClient(cfgBlobName);

      // work with data blobs for this doc
      String latestPath = "ContainerA/" + docId + "/latest.json";
      BlobClient latest = DATA_CONTAINER.getBlobClient(latestPath);

      // ... your compare / version / tag logic here ...
    }
  }

  @FunctionName("TriggerForContainerB")
  public void onB(
      @CosmosDBTrigger(/* ... */) List<ObjectNode> items,
      ExecutionContext ctx) {

    for (ObjectNode doc : items) {
      String docId = doc.get("id").asText();
      String cfgBlobName = System.getenv("TrackedFields_ContainerB");
      BlobClient cfgBlob = CONFIG_CONTAINER.getBlobClient(cfgBlobName);

      String latestPath = "ContainerB/" + docId + "/latest.json";
      BlobClient latest = DATA_CONTAINER.getBlobClient(latestPath);

      // ... logic ...
    }
  }
}