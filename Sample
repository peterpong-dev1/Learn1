given()
    .filter(validationFilter)
    .contentType("application/json")
    .body("{ \"postalCode\": \"90210\" }")
.when()
    .post("/v1/common/address/search")
.then()
    .statusCode(200);

ErrorResponseDTO errorResponse = new ErrorResponseDTO(
    String.valueOf(status.value()),
    message,
    errorDetails,
    OffsetDateTime.now(ZoneOffset.UTC).format(DateTimeFormatter.ISO_OFFSET_DATE_TIME), // formatted String
    MDC.get("requestId")
);