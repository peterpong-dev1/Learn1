import java.util.List;
import java.util.Map;

/**
 * Root POJO representing the full config.
 * Map of event ID to its associated LookupConfig.
 */
public class LookupConfig extends java.util.HashMap<String, EventLookup> {
    private static final long serialVersionUID = 1L;
}

/**
 * Represents an event-specific configuration.
 * Contains a list of entities to look up.
 */
class EventLookup {
    private List<LookupEntity> entities;

    public List<LookupEntity> getEntities() {
        return entities;
    }

    public void setEntities(List<LookupEntity> entities) {
        this.entities = entities;
    }
}

/**
 * Represents a single lookup step in the chain.
 * Includes DB details, query, inputs, and outputs.
 */
class LookupEntity {
    private String name;
    private String database;
    private String container;
    private String query;
    private Map<String, String> params;
    private Map<String, String> outputs;

    public String getName() {
        return name;
    }

    public void setName(String name) {
        this.name = name;
    }

    public String getDatabase() {
        return database;
    }

    public void setDatabase(String database) {
        this.database = database;
    }

    public String getContainer() {
        return container;
    }

    public void setContainer(String container) {
        this.container = container;
    }

    public String getQuery() {
        return query;
    }

    public void setQuery(String query) {
        this.query = query;
    }

    public Map<String, String> getParams() {
        return params;
    }

    public void setParams(Map<String, String> params) {
        this.params = params;
    }

    public Map<String, String> getOutputs() {
        return outputs;
    }

    public void setOutputs(Map<String, String> outputs) {
        this.outputs = outputs;
    }
}

{
  "newBusiness_submitted_agent": {
    "entities": [
      {
        "name": "policy",
        "database": "policy-db",
        "container": "transactionProgress",
        "query": "SELECT c.applicationId, c.agentId FROM c WHERE c.policyId = @policyId",
        "params": {
          "policyId": "policyId"  // from trigger
        },
        "outputs": {
          "applicationId": "applicationId",
          "agentId": "agentId"
        }
      },
      {
        "name": "agent",
        "database": "agent-db",
        "container": "agentDetails",
        "query": "SELECT c.name, c.eMail.address, c.phone, c.lineId FROM c WHERE c.agentId = @agentId",
        "params": {
          "agentId": "agentId"
        },
        "outputs": {
          "name": "recipient.name",
          "address": "recipient.email",
          "phone": "recipient.phone",
          "lineId": "recipient.lineId"
        }
      }
    ]
  },

  "policy_approved_customer": {
    "entities": [
      {
        "name": "policy",
        "database": "policy-db",
        "container": "approvedPolicies",
        "query": "SELECT c.customerId, c.policyNumber FROM c WHERE c.policyId = @policyId",
        "params": {
          "policyId": "policyId"
        },
        "outputs": {
          "customerId": "customerId",
          "policyNumber": "policyNumber"
        }
      },
      {
        "name": "customer",
        "database": "customer-db",
        "container": "customers",
        "query": "SELECT c.name, c.contact.email FROM c WHERE c.customerId = @customerId",
        "params": {
          "customerId": "customerId"
        },
        "outputs": {
          "name": "recipient.name",
          "email": "recipient.email"
        }
      }
    ]
  }
}

