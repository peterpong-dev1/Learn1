It looks like the characterSetType field, which is an enum, might not be correctly handled during request deserialization and query building. Let’s ensure proper handling at all levels.

🔹 Fix for Enum Handling in DTO

Ensure that Jackson can properly map the JSON string to the enum.

✅ Update CharacterSetType Enum

import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonValue;

public enum CharacterSetType {
    KANJI("kanji"),
    KANA("kana");

    private final String value;

    CharacterSetType(String value) {
        this.value = value;
    }

    @JsonValue
    public String getValue() {
        return value;
    }

    @JsonCreator
    public static CharacterSetType fromValue(String value) {
        for (CharacterSetType type : CharacterSetType.values()) {
            if (type.value.equalsIgnoreCase(value)) {
                return type;
            }
        }
        throw new IllegalArgumentException("Invalid characterSetType: " + value);
    }
}

	•	@JsonValue ensures that when returning responses, the JSON contains "kanji" or "kana", instead of "KANJI" or "KANA".
	•	@JsonCreator allows deserialization from JSON string values.

🔹 Fix in AddressDetailsDTO

Ensure that Jackson correctly serializes and deserializes the enum.

import jakarta.validation.constraints.NotNull;
import lombok.Data;

@Data
public class AddressDetailsDTO {
    @NotNull(message = "CharacterSetType must not be null")
    private CharacterSetType characterSetType;

    private String postalCode;
    private String country;
    private String prefecture;
    private String city;
    private String town;
    private String lane;
}

	•	Uses CharacterSetType enum instead of String.

🔹 Fix in Repository Query Handling

The repository must correctly handle enum values while constructing the query.

Before (Buggy Code)

if (StringUtils.hasText(searchConditions.getAddress().getCharacterSetType())) {
    queryBuilder.append(" ").append(queryConfig.getQuery("filterByCharacterSetType"));
    parameters.add(new SqlParameter("@characterSetType", searchConditions.getAddress().getCharacterSetType()));
}

✅ After (Fixed Code)

if (searchConditions.getAddress() != null && searchConditions.getAddress().getCharacterSetType() != null) {
    queryBuilder.append(" ").append(queryConfig.getQuery("filterByCharacterSetType"));
    parameters.add(new SqlParameter("@characterSetType", searchConditions.getAddress().getCharacterSetType().getValue()));
}

	•	getCharacterSetType().getValue() ensures that the query uses the correct string value (kanji/kana) rather than the enum name (KANJI/KANA).

🔹 Fix in Request Validation

Ensure the controller properly validates incoming requests.

Before (Missing Validation)

@PostMapping("/search")
public ResponseEntity<List<AddressDTO>> searchAddress(@RequestBody AddressSearchConditionsDTO searchConditions) {
    List<AddressDTO> results = addressService.searchAddress(searchConditions);
    return ResponseEntity.ok(results);
}

✅ After (Fixed Validation)

@PostMapping("/search")
public ResponseEntity<List<AddressDTO>> searchAddress(
    @Valid @RequestBody AddressSearchConditionsDTO searchConditions) {
    List<AddressDTO> results = addressService.searchAddress(searchConditions);
    return ResponseEntity.ok(results);
}

	•	Ensures automatic validation of characterSetType before calling the service.

🔹 Fix in Test Cases

Modify test cases to validate correct enum behavior.

✅ Add Test Cases

@Test
void testValidCharacterSetType() {
    String json = "{ \"addressCode\": \"123\", \"address\": { \"characterSetType\": \"kanji\" } }";
    AddressSearchConditionsDTO dto = objectMapper.readValue(json, AddressSearchConditionsDTO.class);
    assertEquals(CharacterSetType.KANJI, dto.getAddress().getCharacterSetType());
}

@Test
void testInvalidCharacterSetType() {
    String json = "{ \"addressCode\": \"123\", \"address\": { \"characterSetType\": \"invalidValue\" } }";
    Exception exception = assertThrows(IllegalArgumentException.class, () ->
        objectMapper.readValue(json, AddressSearchConditionsDTO.class)
    );
    assertTrue(exception.getMessage().contains("Invalid characterSetType"));
}

💡 Summary of Fixes

✅ Fixed Enum Serialization/Deserialization
✅ Fixed Null Checks in Repository Query Construction
✅ Validated Enum Field in DTO and Controller
✅ Ensured Proper Enum Handling in Test Cases

Would you like me to update your log statements accordingly? 🚀