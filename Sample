Yes, based on the uploaded Swagger image, the error response structure needs an “Error” object that contains another “error” object inside it.

✅ Updated Error Handling to Match Swagger Format

To match the exact nested error structure, we need to modify our DTOs slightly.

📌 Step 1: Define the Outer ErrorContainerDTO

This will act as the outermost wrapper.

package jp.co.mycompany.servicing.dto;

import lombok.AllArgsConstructor;
import lombok.Data;
import lombok.NoArgsConstructor;

@Data
@AllArgsConstructor
@NoArgsConstructor
public class ErrorContainerDTO {
    private ErrorResponseDTO error;  // Contains the actual error object inside
}

📌 Step 2: Define the Inner ErrorResponseDTO

This is the inner error object that holds the details.

package jp.co.mycompany.servicing.dto;

import lombok.AllArgsConstructor;
import lombok.Data;
import lombok.NoArgsConstructor;

import java.time.LocalDateTime;
import java.util.List;

@Data
@AllArgsConstructor
@NoArgsConstructor
public class ErrorResponseDTO {
    private String code;       // HTTP Status Code (400, 500, etc.)
    private String message;    // General error message
    private List<ErrorDetailDTO> details; // Specific field errors (if applicable)
    private LocalDateTime timestamp; // When the error occurred
    private String requestId;  // Unique request ID (generated in exception handler)
}

📌 Step 3: Modify GlobalExceptionHandler to Use the Nested Error Structure

Now, instead of returning ErrorResponseDTO directly, we wrap it inside ErrorContainerDTO.

package jp.co.mycompany.servicing.exception;

import jp.co.mycompany.servicing.dto.ErrorDetailDTO;
import jp.co.mycompany.servicing.dto.ErrorResponseDTO;
import jp.co.mycompany.servicing.dto.ErrorContainerDTO;
import org.springframework.http.HttpStatus;
import org.springframework.http.ResponseEntity;
import org.springframework.validation.FieldError;
import org.springframework.web.bind.MethodArgumentNotValidException;
import org.springframework.web.bind.annotation.ExceptionHandler;
import org.springframework.web.bind.annotation.RestControllerAdvice;

import java.time.LocalDateTime;
import java.util.List;
import java.util.UUID;
import java.util.stream.Collectors;

@RestControllerAdvice
public class GlobalExceptionHandler {

    private static final String ERROR_REQUEST_ID_PREFIX = "ERR-";

    // 🛑 Handle Validation Errors (400 Bad Request)
    @ExceptionHandler(MethodArgumentNotValidException.class)
    public ResponseEntity<ErrorContainerDTO> handleValidationException(MethodArgumentNotValidException ex) {
        List<ErrorDetailDTO> details = ex.getBindingResult().getAllErrors().stream()
                .map(error -> new ErrorDetailDTO(
                        ((FieldError) error).getField(),
                        error.getDefaultMessage()
                ))
                .collect(Collectors.toList());

        ErrorResponseDTO errorResponse = new ErrorResponseDTO(
                "400",
                "Invalid parameter value",
                details,
                LocalDateTime.now(),
                generateRequestId()
        );

        return new ResponseEntity<>(new ErrorContainerDTO(errorResponse), HttpStatus.BAD_REQUEST);
    }

    // 🛑 Handle Business Logic Errors (e.g., Invalid Input)
    @ExceptionHandler(IllegalArgumentException.class)
    public ResponseEntity<ErrorContainerDTO> handleIllegalArgument(IllegalArgumentException ex) {
        ErrorResponseDTO errorResponse = new ErrorResponseDTO(
                "400",
                "Invalid input",
                List.of(new ErrorDetailDTO("general", ex.getMessage())),
                LocalDateTime.now(),
                generateRequestId()
        );

        return new ResponseEntity<>(new ErrorContainerDTO(errorResponse), HttpStatus.BAD_REQUEST);
    }

    // 🛑 Handle Generic Exceptions (500 Internal Server Error)
    @ExceptionHandler(Exception.class)
    public ResponseEntity<ErrorContainerDTO> handleGenericException(Exception ex) {
        ErrorResponseDTO errorResponse = new ErrorResponseDTO(
                "500",
                "Internal Server Error",
                List.of(new ErrorDetailDTO("general", ex.getMessage())),
                LocalDateTime.now(),
                generateRequestId()
        );

        return new ResponseEntity<>(new ErrorContainerDTO(errorResponse), HttpStatus.INTERNAL_SERVER_ERROR);
    }

    // ✅ Generate a unique requestId for debugging purposes
    private String generateRequestId() {
        return ERROR_REQUEST_ID_PREFIX + UUID.randomUUID().toString();
    }
}

📌 Step 4: Example API Responses

Now, the error response format exactly matches the Swagger structure.

📌 Example: Validation Error (400 Bad Request)

{
  "error": {
    "code": "400",
    "message": "Invalid parameter value",
    "details": [
      {
        "field": "financialInstitutionCode",
        "issue": "Financial Institution Code must be between 3 and 10 characters"
      }
    ],
    "timestamp": "2024-02-28T12:30:00",
    "requestId": "ERR-123e4567-e89b-12d3-a456-426614174000"
  }
}

📌 Example: Internal Server Error (500 Internal Server Error)

{
  "error": {
    "code": "500",
    "message": "Internal Server Error",
    "details": [
      {
        "field": "general",
        "issue": "Unexpected error occurred"
      }
    ],
    "timestamp": "2024-02-28T12:31:00",
    "requestId": "ERR-abcdef12-3456-7890-abcd-ef1234567890"
  }
}

✅ Final Confirmation

✔ Yes, this exactly matches the Swagger error object format you uploaded.
✔ The error response now has an outer error object that contains the actual error details inside.
✔ No need for filters or interceptors—requestId is generated inside the exception handler.

🚀 Next Steps

Would you like:
	1.	Unit tests for GlobalExceptionHandler to verify these responses?
	2.	Logging for errors to store requestId along with error messages?