I’ll update the code to use Lombok annotations for getters, setters, constructors, and ensure enums have all necessary features like @JsonCreator and @JsonValue. Also, I’ll adjust the mapper to match the style used in the previous project.

⸻

Updated DTOs

import lombok.*;

import java.time.LocalDate;
import java.util.List;

@Getter
@Setter
@AllArgsConstructor
@NoArgsConstructor
public class PolicyHolderDTO {
    private String id;
    private String policyId;
    private CustomerType customerType;
    private String customerId;
    private PartyType partyType;
    private List<NameDTO> name;
    private LocalDate birthDate;
    private Gender gender;
    private List<AddressDTO> address;
    private List<String> email;
    private List<PhoneNumberDTO> phoneNumber;
    private List<DigitalTouchpointDTO> digitalTouchpoint;
    private ReceiptOfNotices receiptOfNotices;
    private LocalDate identificationDate;
    private LocalDate effectiveDate;
    private String relationshipFromInsured;
    private String contractAge;
}

import lombok.*;

@Getter
@Setter
@AllArgsConstructor
@NoArgsConstructor
public class NameDTO {
    private CharacterSetType characterSetType;
    private String lastName;
    private String firstName;
    private String previousLastName;
    private String otherName;
    private Boolean namePictorickKanjiFlag;
    private Boolean nameNonStandardKanjiFlag;
}

import lombok.*;

@Getter
@Setter
@AllArgsConstructor
@NoArgsConstructor
public class AddressDTO {
    private CharacterSetType characterSetType;
    private AddressType type;
    private String addressCode;
    private String postalCode;
    private String country;
    private String prefecture;
    private String city;
    private String town;
    private String lane;
    private String buildingName;
    private String floorNumber;
    private String roomNumber;
}

import lombok.*;

@Getter
@Setter
@AllArgsConstructor
@NoArgsConstructor
public class PhoneNumberDTO {
    private PhoneType type;
    private ContactType contactType;
    private String number1;
    private String number2;
    private String number3;
}

import lombok.*;

@Getter
@Setter
@AllArgsConstructor
@NoArgsConstructor
public class DigitalTouchpointDTO {
    private DigitalType type;
    private String digitalId;
}



⸻

Updated Enums

import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonValue;

public enum CustomerType {
    POLICY_HOLDER("policyHolder");

    private final String value;

    CustomerType(String value) {
        this.value = value;
    }

    @JsonValue
    public String getValue() {
        return value;
    }

    @JsonCreator
    public static CustomerType fromValue(String value) {
        for (CustomerType type : values()) {
            if (type.value.equalsIgnoreCase(value)) {
                return type;
            }
        }
        throw new IllegalArgumentException("Invalid customerType: " + value);
    }
}

import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonValue;

public enum PartyType {
    INDIVIDUAL("individual"),
    CORPORATION("corporation");

    private final String value;

    PartyType(String value) {
        this.value = value;
    }

    @JsonValue
    public String getValue() {
        return value;
    }

    @JsonCreator
    public static PartyType fromValue(String value) {
        for (PartyType type : values()) {
            if (type.value.equalsIgnoreCase(value)) {
                return type;
            }
        }
        throw new IllegalArgumentException("Invalid partyType: " + value);
    }
}

import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonValue;

public enum CharacterSetType {
    KANJI("kanji"),
    KANA("kana"),
    ALFABET("alfabet");

    private final String value;

    CharacterSetType(String value) {
        this.value = value;
    }

    @JsonValue
    public String getValue() {
        return value;
    }

    @JsonCreator
    public static CharacterSetType fromValue(String value) {
        for (CharacterSetType type : values()) {
            if (type.value.equalsIgnoreCase(value)) {
                return type;
            }
        }
        throw new IllegalArgumentException("Invalid characterSetType: " + value);
    }
}

(The same structure applies to other enums like Gender, AddressType, PhoneType, etc.)

⸻

Updated Model Classes

import lombok.*;

import java.time.LocalDate;
import java.util.List;

@Getter
@Setter
@AllArgsConstructor
@NoArgsConstructor
public class PolicyHolder {
    private String id;
    private String policyId;
    private CustomerType customerType;
    private String customerId;
    private PartyType partyType;
    private List<Name> name;
    private LocalDate birthDate;
    private Gender gender;
    private List<Address> address;
    private List<String> email;
    private List<PhoneNumber> phoneNumber;
    private List<DigitalTouchpoint> digitalTouchpoint;
    private ReceiptOfNotices receiptOfNotices;
    private LocalDate identificationDate;
    private LocalDate effectiveDate;
    private String relationshipFromInsured;
    private String contractAge;
}

import lombok.*;

@Getter
@Setter
@AllArgsConstructor
@NoArgsConstructor
public class Name {
    private CharacterSetType characterSetType;
    private String lastName;
    private String firstName;
    private String previousLastName;
    private String otherName;
    private Boolean namePictorickKanjiFlag;
    private Boolean nameNonStandardKanjiFlag;
}

import lombok.*;

@Getter
@Setter
@AllArgsConstructor
@NoArgsConstructor
public class Address {
    private CharacterSetType characterSetType;
    private AddressType type;
    private String addressCode;
    private String postalCode;
    private String country;
    private String prefecture;
    private String city;
    private String town;
    private String lane;
    private String buildingName;
    private String floorNumber;
    private String roomNumber;
}

(Similar changes for PhoneNumber, DigitalTouchpoint, etc.)

⸻

Updated Mapper Class

Since you mentioned that the mapper structure was different from what we used before, I’ll adjust it accordingly.

import org.mapstruct.Mapper;
import org.mapstruct.Mapping;
import org.mapstruct.factory.Mappers;
import java.util.List;

@Mapper(componentModel = "spring")
public interface PolicyHolderMapper {
    PolicyHolderMapper INSTANCE = Mappers.getMapper(PolicyHolderMapper.class);

    @Mapping(target = "id", source = "policyHolder.id")
    @Mapping(target = "policyId", source = "policyHolder.policyId")
    PolicyHolderDTO toDTO(PolicyHolder policyHolder);

    @Mapping(target = "id", source = "policyHolderDTO.id")
    @Mapping(target = "policyId", source = "policyHolderDTO.policyId")
    PolicyHolder toModel(PolicyHolderDTO policyHolderDTO);

    List<PolicyHolderDTO> toDTOList(List<PolicyHolder> policyHolders);
    List<PolicyHolder> toModelList(List<PolicyHolderDTO> policyHolderDTOs);
}



⸻

Changes Summary

✔ Lombok Annotations added: @Getter, @Setter, @AllArgsConstructor, @NoArgsConstructor
✔ Enums Improved: Now include @JsonCreator and @JsonValue for JSON parsing.
✔ Mapper Updated: Matches the style used in the previous project with @Mapping.

⸻

This should now match the previous project implementation. Let me know if you need modifications or additional files!