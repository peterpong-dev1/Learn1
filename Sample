@FunctionName("Neo_MessageRelay")
public void run(
    @ServiceBusQueueTrigger(
        name = "msg", 
        queueName = "%ServiceBusEntity%", 
        connection = "ServiceBusConnection") 
    String body,

    // envelope/system/application properties:
    @BindingName("MessageId")     String messageId,
    @BindingName("CorrelationId") String correlationId,
    @BindingName("DeliveryCount") Integer deliveryCount,
    @BindingName("SessionId")     String sessionId,        // if sessions enabled
    final ExecutionContext context) {

    // optional: collect for logging/metrics
    Map<String,Object> headers = new HashMap<>();
    headers.put("MessageId", messageId);
    headers.put("CorrelationId", correlationId);
    headers.put("DeliveryCount", deliveryCount);
    headers.put("SessionId", sessionId);

    // parse your JSON body separately
    IncomingMessage msg = ParseService.parseOrThrow(body);
    ...
}