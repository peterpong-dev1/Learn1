Thanks for the screenshot — it’s clear and useful. Based on the resolution logic you’ve shared (highlighted and red paths), here’s how your lookup-paths.json could look using your earlier discussed structure.

⸻

✅ lookup-paths.json

{
  "1": [
    {
      "database": "api_policy_db",
      "container": "handler",
      "query": "SELECT c.agency.agentId FROM c WHERE c.policyId = @policyId",
      "inputKey": "policyId",
      "inputType": "string",
      "outputField": "agentId",
      "outputType": "string",
      "required": true,
      "returnField": false
    },
    {
      "database": "api_agents_db",
      "container": "agent",
      "query": "SELECT c.eMail.address FROM c WHERE c.agentId = @agentId AND c.eMail.addressValidated = true",
      "inputKey": "agentId",
      "inputType": "string",
      "outputField": "email",
      "outputType": "string",
      "required": true,
      "returnField": true
    }
  ],
  "2": [
    {
      "database": "api_policy_db",
      "container": "customer",
      "query": "SELECT c.eMail.address FROM c WHERE c.policyId = @policyId AND c.eMail.addressValidated = true",
      "inputKey": "policyId",
      "inputType": "string",
      "outputField": "email",
      "outputType": "string",
      "required": true,
      "returnField": true
    }
  ],
  "3": [
    {
      "database": "api_policy_db",
      "container": "unpaidContractInquiry",
      "query": "SELECT c.representativeAgency.agentId FROM c WHERE c.policyId = @policyId",
      "inputKey": "policyId",
      "inputType": "string",
      "outputField": "agentId",
      "outputType": "string",
      "required": false,
      "returnField": false
    },
    {
      "database": "api_policy_db",
      "container": "representativeAgency",
      "query": "SELECT c.eMail.address FROM c WHERE c.agentId = @agentId AND c.eMail.addressValidated = true",
      "inputKey": "agentId",
      "inputType": "string",
      "outputField": "email",
      "outputType": "string",
      "required": false,
      "returnField": true
    }
  ],
  "4": [
    {
      "database": "api_newbusiness_db",
      "container": "appInfoProgressManagement",
      "query": "SELECT c.agency[0].agentRegistrationNumber FROM c WHERE c.applicationId = @applicationId",
      "inputKey": "applicationId",
      "inputType": "string",
      "outputField": "agentRegistrationNumber",
      "outputType": "string",
      "required": true,
      "returnField": false
    },
    {
      "database": "agents_db",
      "container": "agent",
      "query": "SELECT c.eMail.address FROM c WHERE c.agentRegistrationNumber = @agentRegistrationNumber AND c.eMail.addressValidated = true",
      "inputKey": "agentRegistrationNumber",
      "inputType": "string",
      "outputField": "email",
      "outputType": "string",
      "required": true,
      "returnField": true
    }
  ]
}


⸻

📝 Notes:
	•	Path “1” and “2” are from the yellow-highlighted resolution logic.
	•	Path “3” and “4” are based on the red text paths.
	•	@policyId, @applicationId, etc., are expected to be present in your input keys map.
	•	You can add more paths ("5", "6"…) as needed using the same pattern.

Let me know if you’d like:
	•	🔁 These to be pulled from Azure Blob
	•	🧪 Tests
	•	✅ Optional field fallback logic
	•	📄 Documentation/validation

Ready when you are!